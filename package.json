{
  "name": "labx",
  "version": "1.0.0",
  "description": "This is a very simple Makefile which knows how to build Arduino sketches. It defines entire workflows for compiling code, flashing it to Arduino and even communicating through Serial monitor. You don't need to change anything in the Arduino sketches.",
  "main": "apps.js",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "express": "^4.13.3",
    "serialport": "^1.7.4",
    "socket.io": "^1.3.6"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
